# Skills to add (not found locally)

The kickoff (step 3) asks to copy four skills into `.claude/skills/`. As of scaffolding, **none were present on this machine** (`~/.claude/skills/` held only `ojcs-paper` and `remotion-best-practices`). Add them here when available:

| Skill | Purpose in this repo |
|---|---|
| `ai-asset-generation` | Generate the squirrel renders with set consistency (hero portrait, turnaround, expression sheet). |
| `ai-render-keying` | Key GREEN/WHITE renders → transparent PNGs into `assets/keyed/`. Clears the keying backlog. |
| `sprite-animation` | Build walk/run/boo/hang sprite sequences from keyed frames. |
| `immersive-html` | Re-skin / extend `site/index.html` (Three.js hero, starfield, RAF state-machine squirrel). |

Drop each skill as `.claude/skills/<name>/SKILL.md` (+ supporting files). Once present, they're auto-discovered.
